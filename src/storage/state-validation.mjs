import {validateHubPage,validateHubOverlays} from '../content-hub/validation.mjs';
import {validateCheatsheetPage} from '../cheatsheets/validation.mjs';
import {validatePersonal} from './personal-validation.mjs';
import {validateCompanion,companionKey} from '../companion/validation.mjs';
import {inspectObject,ID,safeUrl,validateSchema} from '../core/validation.mjs';
/** Validate restored personal state before any IndexedDB write. Never repair by dropping fields. */
export function validateState(ws,schemas){
 inspectObject(ws);const fail=m=>{throw Error('Invalid saved workspace: '+m);};
 const obj=(x,n)=>{if(!x||typeof x!=='object'||Array.isArray(x))fail(n);};
 const str=(x,n,max=1000000)=>{if(typeof x!=='string'||x.length>max)fail(n);};
 const num=(x,n,lo=0,hi=Number.MAX_SAFE_INTEGER)=>{if(!Number.isFinite(x)||x<lo||x>hi)fail(n);};
 const bool=(x,n)=>{if(typeof x!=='boolean')fail(n);};
 const id=(x,n)=>{if(typeof x!=='string'||!ID.test(x))fail(n);};
 const arr=(x,n,max=100000)=>{if(!Array.isArray(x)||x.length>max)fail(n);};
 const anchor=a=>{if(a===undefined)return;obj(a,'anchor');if(a.blockId!==undefined)id(a.blockId,'anchor block');if(a.offset!==undefined)num(a.offset,'anchor offset');if(a.unit!==undefined)str(a.unit,'source unit',256);if(a.viewportOffset!==undefined)num(a.viewportOffset,'anchor viewport offset',-1000000,1000000);if(a.atStart!==undefined)bool(a.atStart,'anchor start');if(a.pdfPage!==undefined)num(a.pdfPage,'physical PDF page',1,1000000);if(a.pdfRevision!==undefined)str(a.pdfRevision,'PDF revision',256);};
 const p=ws?.personal,o=ws?.overlays;obj(o,'overlays');validateHubOverlays(o);if(o.schemaVersion!==2)fail('schema version');validatePersonal(p);
 obj(o.pages,'page overlays');arr(o.projects,'local projects');obj(o.projectPrefs,'project preferences');arr(o.operations,'structural operations');arr(o.archived,'archives');o.archived.forEach(x=>id(x,'archived ID'));arr(o.documents,'local documents');if(o.groups!==null)arr(o.groups,'groups');
 for(const [key,edit] of Object.entries(o.pages)){obj(edit,'page overlay');if(edit.page?.id!==key)fail('overlay ID mismatch');validateCheatsheetPage(edit.page);validateHubPage(edit.page);validateSchema({schemaVersion:2,title:'Local overlay',projects:[],pages:[edit.page],glossary:[],groups:[]},schemas.v2,'local overlay');if(edit.baseHash!==undefined&&!/^[a-f0-9]{64}$/.test(edit.baseHash))fail('overlay base hash');}
 validateSchema({schemaVersion:2,title:'Local tree',projects:o.projects,pages:[],glossary:[],groups:o.groups??[]},schemas.v2,'local tree');
 for(const [key,pref] of Object.entries(o.projectPrefs)){id(key,'project preference');obj(pref,'project preference');for(const k of ['title','description','icon'])if(pref[k]!==undefined)str(pref[k],k,10000);if(pref.hidden!==undefined)bool(pref.hidden,'hidden');if(pref.order!==undefined)num(pref.order,'order',-100000,100000);}
 for(const op of o.operations){obj(op,'operation');if(!['add','move','rename','order'].includes(op.kind))fail('operation kind');id(op.nodeId,'operation ID');for(const k of ['projectId','parentId'])if(op[k])id(op[k],k);if(op.kind==='rename')str(op.title,'node title',10000);if(op.kind==='order')num(op.delta,'order delta',-10000,10000);if(['add','move'].includes(op.kind))id(op.projectId,'destination project');if(op.kind==='add'){if(op.node?.id!==op.nodeId)fail('added node ID');validateSchema({schemaVersion:2,title:'Node',projects:[{id:'project.validation',title:'Validation',icon:'book',description:'',nodes:[op.node]}],pages:[],glossary:[],groups:[]},schemas.v2,'local node');}}
 for(const d of o.documents){id(d.id,'local document');id(d.pageId,'document page');str(d.title,'document title',10000);if(!['public','private','unreviewed'].includes(d.visibility))fail('document visibility');obj(d.rights,'document rights');str(d.rights.attribution,'attribution',10000);obj(d.source,'document source');if(!['local','library-file','pack-file','https','external-link'].includes(d.source.kind))fail('document source kind');if(d.source.url&&!safeUrl(d.source.url))fail('document URL');if(d.sha256&&!/^[a-f0-9]{64}$/.test(d.sha256))fail('document hash');if(d.assetKey!==undefined)str(d.assetKey,'document asset key',600);}
 if(o.categories!==undefined){obj(o.categories,'categories');for(const [key,value] of Object.entries(o.categories)){id(key,'category project');if(value!==null&&!['informatics','cloud','norsk','job','personal'].includes(value))fail('category');}}
 if(o.glossary!==undefined){arr(o.glossary,'promoted terms',5000);validateSchema({schemaVersion:2,title:'Local glossary',projects:[],pages:[],glossary:o.glossary.map(({pdfRefs,tags,...t})=>t),groups:[]},schemas.v2,'local glossary');const ids=new Set();for(const t of o.glossary){if(ids.has(t.id))fail('duplicate local term');ids.add(t.id);if(t.tags!==undefined){arr(t.tags,'local term tags',100);for(const tag of t.tags)str(tag,'local term tag',256);}if(t.pdfRefs!==undefined){arr(t.pdfRefs,'PDF term backlinks',100);for(const r of t.pdfRefs){obj(r,'PDF backlink');for(const k of Object.keys(r))if(!['pageId','documentId','revision','pages'].includes(k))fail('unknown PDF backlink field');id(r.pageId,'PDF backlink page');id(r.documentId,'PDF backlink document');if(r.revision!==undefined&&!/^[a-f0-9]{64}$/.test(r.revision))fail('PDF backlink revision');arr(r.pages,'PDF backlink occurrences',10000);for(const n of r.pages){num(n,'PDF backlink physical page',1,10000);if(!Number.isInteger(n))fail('PDF backlink physical page');}}}}}
 if(o.companions!==undefined){obj(o.companions,'companions');if(Object.keys(o.companions).length>500||JSON.stringify(o.companions).length>20000000)fail('companion storage limit');for(const [key,c] of Object.entries(o.companions)){validateCompanion(c);if(key!==companionKey({id:c.documentId,sha256:c.documentSha256}))fail('companion revision key');}}
 return true;
}
