import React,{useState} from '../vendor/react.mjs';import type {Catalogue,TreeNode,Project,Workspace} from '../core/model.js';import {Icon,IconButton} from './Icon.js';import {normalize} from '../core/workspace.js';
export function ProjectTree({catalogue:c,workspace:ws,activePage,onOpen,onToggle,onItem,onCreate}:any){const [filter,setFilter]=useState('');const expanded=new Set(ws.personal.session.expanded),archived=new Set(ws.overlays.archived),query=normalize(filter);
 const matches=(n:TreeNode):boolean=>!query||normalize(n.title).includes(query)||!!n.children?.some(matches);const hidden=(p:Project)=>ws.overlays.projectPrefs[p.id]?.hidden||archived.has(p.id);
 function nodes(ns:TreeNode[],p:Project,depth:number){return ns.filter(n=>!archived.has(n.id)&&!archived.has(n.pageId)&&matches(n)).map(n=>{const open=expanded.has(n.id)||!!query,page=c.pages.find((p:any)=>p.id===n.pageId);return <div className="tree-node" key={n.id}><div className={'tree-row '+(n.pageId===activePage?'active':'')} style={{paddingLeft:(10+depth*14)+'px'}}><button className="tree-target" title={n.title} aria-expanded={n.children?open:undefined} aria-current={n.pageId===activePage?'page':undefined} onClick={e=>n.pageId?onOpen(n.pageId,undefined,e.ctrlKey||e.metaKey):onToggle(n.id)} onAuxClick={e=>{if(e.button===1&&n.pageId){e.preventDefault();onOpen(n.pageId,undefined,true);}}}>{n.children?<Icon name={open?'down':'chevron'} size={12}/>:<span className="tree-indent"/>}<Icon name={n.children?'folder':c.documents.some((d:any)=>d.pageId===n.pageId)?'pdf':'page'} size={15}/><span>{page?.title??n.title}</span></button>{n.pageId&&ws.personal.session.showFlags&&ws.personal.ratings[n.pageId]&&<span className={'flag-dot '+ws.personal.ratings[n.pageId]} title={ws.personal.ratings[n.pageId]}/>}<IconButton name="more" label={'Manage '+n.title} className="tree-more" onClick={()=>onItem({project:p,node:n})}/></div>{n.children&&open&&<div className="tree-children">{n.children.length?nodes(n.children,p,depth+1):<button className="empty-folder" style={{marginLeft:(30+depth*14)+'px'}} onClick={()=>onCreate('page',p,n)}>Add a page</button>}</div>}</div>;});}
 function project(p:Project){if(hidden(p))return null;const open=expanded.has(p.id)||!!query;if(query&&!normalize(p.title).includes(query)&&!p.nodes.some(matches))return null;return <div className="tree-project" key={p.id}><div className="tree-row project-row"><button className="tree-target" onClick={()=>onToggle(p.id)} aria-expanded={open} title={p.title}><Icon name={open?'down':'chevron'} size={12}/><span className="project-icon"><Icon name={p.icon} size={17}/></span><strong>{p.title}</strong></button><IconButton name="plus" label={'Add to '+p.title} className="tree-more" onClick={()=>onCreate('page',p)}/></div>{open&&nodes(p.nodes,p,0)}</div>;}
 const grouped=new Set(c.groups.flatMap((g:any)=>g.projectIds));return <aside className="library-sidebar" aria-label="Notebook library"><div className="sidebar-heading"><strong>My notebooks</strong><span className="secondary">{c.projects.filter((p:any)=>!hidden(p)).length}</span></div><div className="tree-filter"><Icon name="search" size={15}/><input aria-label="Filter notebook tree" placeholder="Filter notebooks" value={filter} onChange={e=>setFilter(e.target.value)}/>{filter&&<IconButton name="close" label="Clear tree filter" onClick={()=>setFilter('')}/>}</div><nav className="tree-scroll" aria-label="Projects and pages">{c.groups.map((g:any)=><section className="tree-group" key={g.id}><h3>{g.title}</h3>{g.projectIds.map((id:string)=>c.projects.find((p:any)=>p.id===id)).filter(Boolean).map(project)}</section>)}{c.projects.filter((p:any)=>!grouped.has(p.id)).length>0&&<section className="tree-group"><h3>NOTEBOOKS</h3>{c.projects.filter((p:any)=>!grouped.has(p.id)).map(project)}</section>}</nav><button className="sidebar-add" onClick={()=>onCreate('project')}><Icon name="plus" size={16}/> New notebook</button><div className="local-status"><span className="status-dot"/> Stored on this device</div></aside>;}
