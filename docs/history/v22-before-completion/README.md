# Historical documents, not current acceptance evidence

These files are exact copies of the entrypoints in the supplied V2.2 source before
this coding pass. Their old dependency/browser blockers and test counts describe
an earlier candidate, not the source in the current delivery.

The newer input handoff reports later successful integrated CI/browser work at
`60db2cb7504503ac416ddad940441e24f82266b1`. That evidence also predates the new code.
Read the current root `START_HERE.md` and `V22_TEST_EVIDENCE.md` for this delivery.
