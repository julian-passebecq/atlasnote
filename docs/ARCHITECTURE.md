# AtlasNote V2.2 architecture

The current source of truth is [the architecture index](ARCHITECTURE_INDEX.md) and [V22_ARCHITECTURE.md](../V22_ARCHITECTURE.md).

The old four-store/optional-PDF description is retained verbatim in `history/v21-entrypoints/ARCHITECTURE.md`; it is not the current migration or release contract. V2.2 adds one history store to the existing database and requires the integrated React-PDF build for production. A compatibility build cannot certify that engine.
